prefix-api=/api/v1
prefix=/api

logging.level.org.springframework.security.web.FilterChainProxy=ERROR

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
# This will help us generate the initial DDL script on startup
spring.jpa.generate-ddl=true

logging.level.org.hibernate =ERROR

# App Properties
bezkoder.app.jwtSecret= 7hdX#is90sn2
bezkoder.app.jwtExpirationMs= 86400000

logging.level.org.springframework.web.socket.config.WebSocketMessageBrokerStats=ERROR
logging.level.org.springframework=ERROR
logging.level.org.springframework.boot.autoconfigure.logging=ERROR